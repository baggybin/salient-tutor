# Screenshots

Drop captures here — the top-level README references:

- `coach.png` — the Coach workspace (streaming conversation + skill-map rail)
- `library.png` — the Library tab (study cards with fact counts + delete)
- `settings.png` — the ⚙ Settings modal (embeddings + librarian/parser tabs)

Any missing image renders as a broken link until added; no other change needed.
